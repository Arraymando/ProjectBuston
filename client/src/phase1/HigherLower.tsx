export const HigherLower = (props: any) => {
  const { payload } = props;
  const {
    cardDeck,
    setCardDeck,
    randomCard,
    getRandomCard,
    redOrBlackCorrect,
    setRedOrBlackCorrect,
    correctCounter,
    setCorrectCounter,
    gameState,
    setGameState,
    playerHand,
    setPlayerHand,
    correct,
    setCorrect,
  } = payload;

  const higherlowerCheck = () => {};

  return <HigherLower />;
};
